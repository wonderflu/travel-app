extends index
block title
    h1 Hi, #{name}
block description
    p hellonest #{resetToken}
    a(href=`${process.env.SERVER_URL}:${process.env.PORT}/api/v1/users/resetPassword/${resetToken}`) Reset your password